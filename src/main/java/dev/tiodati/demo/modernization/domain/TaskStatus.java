package dev.tiodati.demo.modernization.domain;

/**
 * Status of a task in the system.
 * Represents the workflow states a task can be in.
 */
public enum TaskStatus {
    OPEN,
    IN_PROGRESS,
    COMPLETED
}
